---

- hosts: all
  tasks:
  - name: install apache & php package
    package:
      name:
        - "{{ apache_package }}"
        - "{{ php_package  }}"
      state: latest
      update_cache: true
